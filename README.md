# Goof
Goof Programming Language

staticaly typed --> Specify type on declaration

Int --> long long int
Float --> long double
String --> String
==  >   <  <=  >=  !=
Identifiers need to start with a letter
letters numbers and _ only allowed in identifiers


factorial example code:

Int x = Int(in());
If(x<0) { out("number must be bigger than 0!"); }
Int ans = 1;
Loop(x > 0)
{
    ans = ans * x;
    x--;
}
out(ans);

Parsing:
either

get Tokens:
    Get rid of whitespaces while identifying token type
Parse Tokens:
    

Syntax :
    - Semicolon at the end of each line
    - 
